#include <stdio.h>

#define ARRAYSZ 1000

int a[10] = {0, 2, 4};


int main() {
    
    // for(int i = 0; i < 10; i++) {
    //     a[i] = i * i;
    
    // }

    // for(int i = 0; i < 10; i++) {
    //     printf("a[%d] = %d\n", i, a[i]);
    // }

    // Don't need to initialize the entire array.
    int b[10] = {0, 1, 2, 3, 4};

    // String: Character Arrays
    char buf[12] = "Hello World!";
    char other[12] = "Something";
    printf("%s\n", buf);


    // Constants: literals, defined, and declared

    return 0;
}